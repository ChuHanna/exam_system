<% provide :title, t(".title") %>
<% provide :button_text, t(".button_text") %>
<div class="d-flex align-items-stretch grid-margin">
  <div class="flex-grow">
    <div class="card">
      <div class="card-body">
        <h4 class="card-title"><%= t ".edit_user" %></h4>
        <%= render "form", user: @user %>
      </div>
    </div>
  </div>
</div>

